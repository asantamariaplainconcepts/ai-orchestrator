# CLAUDE.md

This repository's AI agent instructions are tool-neutral and live in
**[`AGENTS.md`](./AGENTS.md)** — read that. Claude Code looks for `CLAUDE.md` specifically, so
this file exists only to point there. Do not add instructions here; edit `AGENTS.md` so every
agent (Claude Code, opencode, Copilot) stays in sync.
