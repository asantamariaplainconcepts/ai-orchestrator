# .NET
bin/
obj/
artifacts/
*.user

# Node
node_modules/
dist/

# Generated: `pnpm build` copies the SPA here for same-origin serving
src/root/AiOrchestrator.Server/wwwroot/

# Framework kit — internal reference material, local-only tooling (DEC-042)
docs/framework/

# Telemetry — private, never in the public repo (DEC-022)
.telemetry/
sessions.jsonl

# Local env
.env
.env.*
*.local

# Loop/scheduler runtime state — local, not source
.claude/scheduled_tasks.lock

# OS/editor
.DS_Store
.idea/
.vscode/

# Terraform — state carries generated credentials in cleartext, and tfvars carries the
# subscription id. Neither may reach this public repository (BR-010, DEC-022 posture).
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
backend.hcl
crash.log
