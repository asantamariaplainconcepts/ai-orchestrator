# Tasks — connector-health

- [x] 1.1 `GET /api/connectors` read slice in Backlog.
- [x] 2.1 Projects list: four-state pill + failure hover + sync age; copy; lint + build.
- [x] 3.1 Functional tests: the four states in one listing; the failure sentence carried;
      BR-010 (no secret value anywhere in the response).
- [x] 4.1 CI's filtered command locally; CI green.
