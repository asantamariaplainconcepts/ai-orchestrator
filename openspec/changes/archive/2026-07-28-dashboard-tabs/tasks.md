# Tasks — dashboard-tabs

- [x] 1.1 shadcn Tabs (+ any primitives the sections need) added via the CLI; the tab scaffold
      on the project page with URL state (design D1) and the derived landing tab (design D3).
- [x] 2.1 Operate tab: pulse strip restyled to shadcn (D2's paid restyle), backlog with
      per-row actions, connector reduced to its health pill.
- [x] 3.1 Runs and Automations tabs: lists migrated to the theme; creation behind
      "New automation".
- [x] 4.1 Settings tab: connector collapsed to one line when configured; full form when
      absent or editing.
- [x] 5.1 Mobile: bottom-docked tab list under md; stories and runs as stacked cards (D4);
      375px pass verified.
- [x] 6.1 E2E reachability green (defaults button, runtime picker, vendor picker — ADR-0006);
      design validator green; mock intact; CI green; evidence on #109.
