# Tasks — defaults-full-catalogue

- [ ] 1.1 Two entries in the default set, ordered as the workflow reads; propose listens on the
      grill's ready label (design D1).
- [ ] 1.2 Tests: six seeded; the upgrade path (four → six, two created, four handled); the
      seeded chain fires; all six labels ensured; approval only on implement.
- [ ] 1.3 ARCHITECTURE.md notes that the seeded set is a working pipeline; CI's filtered command
      locally; CI green.
