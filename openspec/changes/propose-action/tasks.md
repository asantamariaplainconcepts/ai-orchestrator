# Tasks — propose-action

## 1. The action

- [ ] 1.1 `AutomationAction.ProposeSpec`; executor routes it down the implement pipeline with a
      proposal prompt and PR framing; the two refusals run before Prepare.

## 2. Portal

- [ ] 2.1 Action in the catalogue lists; lint + build.

## 3. Tests

- [ ] 3.1 Functional: proposal PR published and recorded; bodyless Story refused pre-workspace;
      linked change refused naming it.

## 4. Close-out

- [ ] 4.1 UC-025 in the corpus (DEC-048 already licenses the growth); ARCHITECTURE.md; CI's
      filtered command locally; CI green.
