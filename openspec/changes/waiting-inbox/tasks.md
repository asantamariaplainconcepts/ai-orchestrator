# Tasks — waiting-inbox

- [ ] 1.1 `GET /api/inbox` in the Runs module: three states, cross-project, derived
      waits-on-nobody for failures (design D2), per-state age (D3), titles via Contracts (D4).
- [ ] 2.1 Inbox page + shell nav entry with the ambient count from the same query (D1); copy;
      lint + build.
- [ ] 3.1 Functional tests: the six scenarios, including the newer-run exclusion both ways.
- [ ] 4.1 E2E reachability: the nav entry exists and the page renders (ADR-0006).
- [ ] 5.1 UC-026 into the corpus; CI's filtered command locally; CI green.
