# Tasks — catalogue-and-workflow

- [ ] 1.1 `workflowGraph` answers membership from the edges (design D2): in the workflow when it
      hands work to another or another hands to it. No flag, no position.
- [ ] 2.1 `AutomationsSection` becomes two named sections (design D1): the catalogue with every
      Automation and every existing action, the workflow with the chain only.
- [ ] 3.1 The flow is one row scrolling inside its own container, never wrapping, and the page body
      never scrolls sideways (design D3). Vertical below the wide breakpoint.
- [ ] 4.1 The workflow's header states its steps and its human stops, both derived (design D4).
- [ ] 5.1 Empty states: a project whose Automations chain to nothing shows the catalogue and a
      workflow empty state saying what would make a flow — not an error, not a blank.
- [ ] 6.1 DEC-053 recorded and the glossary gains **catalogue** and **workflow** (design D5).
- [ ] 7.1 Four states in both sections, both themes, copy through the i18n catalogue, focus visible.
- [ ] 8.1 CI green; evidence on #136.
